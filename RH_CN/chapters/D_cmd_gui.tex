% 附录 D — 命令与 GUI 参考（精译重写）
\biappendix{命令与 GUI 参考}{Command and GUI Reference}
\label{chap:cmd}

\section{引言}
\subsection*{Introduction}

本附录描述 RedHawk 的两种用户界面：TCL 命令行与图形用户界面（GUI）。

\section{启动 RedHawk}
\subsection*{Invoking RedHawk}

在 UNIX 工作目录下执行：
\begin{lstlisting}
redhawk [-b <cmnd_file>] [-c <cmnd_file>] [-f <cmnd_file>]
        [-i ?<cmnd_file>?] [-h] [-tclsh] [-lmhold <cmdfile>]
        [-lmwait <wait_sec>] [-iconify]
        [-style [CDE|Motif|Plastique|Windows]]
        [-stack [<stacksize_MB>|unlimited]]
        [clampviewer <IV_FileName> [<IV_Name(s)>] [<options>]]
\end{lstlisting}

\begin{itemize}
\item \texttt{-b} — 批处理运行 TCL 脚本，不显示 GUI，结束后退出
\item \texttt{-c} — 预解析 TCL 脚本并报告语法警告/错误，不执行
\item \texttt{-f} — 运行 TCL 脚本并显示 GUI
\item \texttt{-i} — 交互 TCL 命令行，无 GUI；须显式退出命令才结束
\item \texttt{-h} — 显示启动语法帮助
\item \texttt{-tclsh} — 完整 TCL shell；检出许可证后可用 \cmd{redhawk} 启动 GUI
\item \texttt{-lmhold} — 退出后保持许可证占用并重新显示 shell
\item \texttt{-lmwait} — 无可用许可证时等待（可指定秒数，超时则退出）
\item \texttt{-iconify} — 启动时最小化 GUI（仍需 DISPLAY 环境）
\item \texttt{-style} — 更改 GUI 窗口管理器风格（默认 Plastique）
\item \texttt{-stack} — 指定栈大小（MB）或 unlimited
\end{itemize}

\section{终止进程}
\subsection*{Terminating Processes}

\texttt{Ctrl+C} 可终止当前操作及多数子进程；在 X-term 中可能直接杀死主进程。
\cmd{setup design} 与提取等少数流程在 GUI 中不响应 Ctrl+C。

\section{TCL 语法约定}
\subsection*{TCL Syntax Conventions}

语法约定同附录 C「关键字语法约定」。

\section{TCL 命令概要}
\subsection*{TCL Command Summary}

TCL 脚本可手动编写，也可通过 GUI Playback 录制。使用 \cmd{help <命令名>} 查看完整语法。
GUI 中支持 TAB 自动补全（按 TAB 可展开命令及下一级子选项）。

下表列出 35 个一级 TCL 命令及中文说明。

\begin{longtable}{@{}p{0.22\textwidth}p{0.72\textwidth}@{}}
\toprule
\textbf{命令} & \textbf{用途说明} \\
\midrule
\endhead
\cmd{cell\_swap} & 将高功耗单元替换为低功耗单元，改善高 IR 压降区域。 \\
\cmd{characterize} & 运行 APL 表征，生成动态 Vdd 电流波形并表征去耦单元。 \\
\cmd{condition} & 设置、查看或清除 DvD 后处理分析条件（plot/print 等）。 \\
\cmd{config} & 配置 GUI 显示选项（色图、按键绑定、层/网络视图等）。 \\
\cmd{decap} & 对电源网格上的去耦电容执行添加、删除或优化。 \\
\cmd{dump} & 将 RedHawk 内部数据库或网络信息导出为指定格式。 \\
\cmd{eco} & 工程变更（ECO）：添加/删除焊盘、绑带、开关、过孔等。 \\
\cmd{export} & 以第三方工具可读格式导出 RedHawk 数据。 \\
\cmd{fao} & 焊盘阵列优化（FAO）：在网格上添加/优化 P/G 焊盘布局。 \\
\cmd{generate} & 生成供复查或后续流程使用的指定文件。 \\
\cmd{get} & 查询并获取设计对象属性（单元、网络、焊盘、开关等）。 \\
\cmd{gsr} & 读取、设置或转储 GSR 关键字值。 \\
\cmd{help} & 列出可用 TCL 命令，或显示指定命令的完整语法。 \\
\cmd{help} & 列出可用 TCL 命令，或显示指定命令的完整语法。 \\
\cmd{history} & 显示或管理当前会话的 TCL 命令历史。 \\
\cmd{import} & 导入 GSR、LEF、DEF、SPEF、STA、工艺、VCD/FSDB 等输入文件。 \\
\cmd{license\_get} & 选择并持有特定类型的 RedHawk 许可证。 \\
\cmd{marker} & 在版图中添加或删除十字准线标记。 \\
\cmd{mesh} & 修改电源网格（绑带、过孔、电阻等）。 \\
\cmd{message} & 显示 Error、Info、Warning 消息信息。 \\
\cmd{movie} & 设置或播放基于实例/晶体管引脚的动态波形动画。 \\
\cmd{perform} & 执行核心分析：功耗计算、提取、DvD、EM、热、ESD 等。 \\
\cmd{pfs} & PathFinder 静电放电（ESD）检查相关功能。 \\
\cmd{plot} & 生成图形化波形或结果曲线文件。 \\
\cmd{print} & 打印基于文本的分析报告。 \\
\cmd{probe} & 在仿真前选择/取消选择探测节点。 \\
\cmd{query} & 查询设计对象并返回属性信息。 \\
\cmd{report} & 生成功耗、EM、IR 压降等文本报告。 \\
\cmd{ring} & 创建或修改模块/芯片周围的电源/地环。 \\
\cmd{route\_fix} & 在指定层上添加修复布线以改善 IR。 \\
\cmd{select} & 在 GUI 中选择并高亮设计对象。 \\
\cmd{setup} & 设置设计数据与运行条件（design、package、pad 等）。 \\
\cmd{show} & 在 GUI 中显示指定结果的色阶图。 \\
\cmd{window} & 控制 GUI 窗口布局与视图配置。 \\
\cmd{zoom} & 缩放版图视图至区域、对象或全芯片。 \\
\bottomrule
\end{longtable}

\section{TCL 命令详解}
\subsection*{TCL Command Details}

以下按原书结构逐条译出各 TCL 命令的语法、选项说明与示例。

\subsection{\cmd{help}}
\subsubsection*{help}

列出可用 TCL 命令，或显示指定命令的完整语法。

\subsection{\cmd{help}}
\subsubsection*{help}

列出可用 TCL 命令，或显示指定命令的完整语法。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
help <TCL_command_name>
\end{lstlisting}

\subsection{\cmd{cell\_swap}}
\subsubsection*{cell swap}

将高功耗单元替换为低功耗单元，改善高 IR 压降区域。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
cell swap <type_of_cells> <cell_name_list>
\end{lstlisting}

\subsection{\cmd{characterize}}
\subsubsection*{characterize}

运行 APL 表征，生成动态 Vdd 电流波形并表征去耦单元。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
characterize apl ? <options> ? <configFileName>
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{-l <cellistFileName>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o <outputFileName>} — 指定 results 文件, with 默认 <单元>.current.（默认：<cell>）
\item \texttt{<confFileName>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-c} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-p <decapFileName>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-l <cellistFileName>} — 指定 单元 list 文件
\item \texttt{-o <outputFileName>} — 指定 results 文件, with 默认 <单元>.cdev.（默认：<cell>）
\end{itemize}

\subsection{\cmd{condition}}
\subsubsection*{condition}

设置、查看或清除 DvD 后处理分析条件（plot/print 等）。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
condition [ set | get | unset ] ? <options> ?
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{-xy <x1 y1 x2 y2>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-time <start> <end>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-type [all |combinational |sequential| clock| memory]} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-timestep <timestep>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-xy} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-time} — 显示 the selected 仿真时间 range,
\item \texttt{-type} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-timestep} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-xy} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-time} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-type} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-timestep} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-all} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\end{itemize}

\subsection{\cmd{config}}
\subsubsection*{config}

配置 GUI 显示选项（色图、按键绑定、层/网络视图等）。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
config [ colormap | keybind | stack | viewlayer | viewnet | viewpad | viewlegend ]
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{-map <colormap ID>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-min <value> -max <value>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-wire | -instance} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-percent | -absolute} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-enable all} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-values \{<list>\}} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-gridsize \{x y\}} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-gridnum \{x y\}} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-refmax} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-hide\_inst \{<instance\_list>\}} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<size\_in\_MB>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-name} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：-）
\item \texttt{-style} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：s - via = invisible, metal = fill, insta）
\item \texttt{-name} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：- all）
\item \texttt{-mode} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：- on）
\item \texttt{-type} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：- all）
\item \texttt{-mode} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：- on）
\end{itemize}

\textbf{示例：}
\begin{lstlisting}
config colormap -map EM -values {0 0 0 75 80 85 90 95 100} -disable {0 1 2 3 }
-refvdd <value> - specifies the reference VDD value from which to scale the color range
-hide_inst {<instance_list>} : hides specific instances from instance based GUI maps. An
config keybind : displays all existing keybindings
config keybind Ctrl+A : displays the TCL procedure of the “Ctrl+A” keybinding
config keybind Ctrl+A export dbA dbB : defines the “Ctrl+A” keybinding, and replaces the
\end{lstlisting}

\subsection{\cmd{decap}}
\subsubsection*{decap}

对电源网格上的去耦电容执行添加、删除或优化。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
decap [ advise | place | fill | remove | fix | report ] ? <options>?
\end{lstlisting}

\subsection{\cmd{dump}}
\subsubsection*{dump}

将 RedHawk 内部数据库或网络信息导出为指定格式。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：settings）

\textbf{语法：}
\begin{lstlisting}
dump [ gif | fao | macro_pin_voltage | memory_via | mmx_pin_info | node_count | ptvoltage |
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{-map <colorMapID>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o <outputFileName>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-nolegend} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：- prints logo / legend）
\item \texttt{-mesh | -decap} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o <tcl\_file>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-inst} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：all instances)）
\item \texttt{-gds [ 0| 1]} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：0)）
\item \texttt{-o} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：file:）
\item \texttt{-o <file\_name>} — 指定 output 文件name
\item \texttt{-cells \{ <master\_cell\_list> \}} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-avg\_volt\_tw <tstart> <tend>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o <outfile>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-box <llx>, <lly>, <urx>, <ury>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-report\_disconnect} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-wstpgarc} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-j <threads>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：is 1）
\item \texttt{-inst\_file} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-box} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o <outFile>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-aveTW} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-maxTW} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-minTW} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-mincyc} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-include\_missing\_TW} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-min\_max} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o} — 指定 output 文件name
\item \texttt{-compress} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-exclude\_region} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-box} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-nets} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-nets\_file} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-layers} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ignore\_layers} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-conf} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-all} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\end{itemize}

\textbf{示例：}
\begin{lstlisting}
-o <outputFileName> : specifies the GIF output file name.
#<InstName> <Subckt Name> <Power Domain> <Ground Domain>
<Worst VDD-VSS> <Time(ns)> <VDD> <VSS> <VDD Node> <VSS Node>
#<InstName:PinName> <Effective Current(A)> <PRatio> (<llx lly>) (<urx ury>)
<TransistorName>
where ‘(<llx lly>)’ and ‘(<urx ury>)’ represent the lower left and upper right x,y corners of the pin geometry.
#<InstName:PinName> <Effective Current(A)> <PRatio> (<llx lly>) (<urx ury>)
<TransistorName> <Disconnected>
#<inst_name:pg_pin_name> <x_loc> <y_loc> <min_volt_tw@time>
<max_volt_tw@time> <avg_volt_tw> <xtor_name>
where ‘<x_loc> <y_loc>’ represent the center x,y coordinates of the pin.
dump via_count -o <filename>
dump wire_count -o <filename>
dump pwrbytile -nx <number_of_tiles_in_x_dir> -ny
<number_of_tiles_in_y_dir> -o power_density.txt
Line2 : < number of grids created> <nx> <ny> ( eg : 400 20 20 )
Line 3 onwards : <lower_left_x> <lower_left_y> <upper_right_x>
<upper_right_y> <power in micro watts>
\end{lstlisting}

\subsection{\cmd{eco}}
\subsubsection*{eco}

工程变更（ECO）：添加/删除焊盘、绑带、开关、过孔等。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
eco [ add [ clamp_cell | clamp_instance |decap | pad | strap | stackvia | via |
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{-width <W>} — 指定 单元 width
\item \texttt{-height <H>} — 指定 单元 height
\item \texttt{-vdd\_pin} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-gnd\_pin} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-cell <cell\_name>} — 指定 master 单元 name of the 实例
\item \texttt{-pin} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-net} — 指定 网络 name of each 引脚
\item \texttt{-origin <x y>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-orient} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-metal <metalLayer>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-decap <cellName>} — 指定 去耦电容 单元 name.
\item \texttt{-power <x y>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ground <x y>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-metal <metal\_layer>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-type [power | ground]} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-x <x\_coord>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-y <y\_coord>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-r <Res>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-c <Cap>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-l <Ind>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-use\_net\_name} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-metal <metalLayer>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-type [power | ground]} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-net} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（必需）
\item \texttt{-[horizontal| vertical ]} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-start} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-end} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-width <width>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-novias} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-toplayer “<netname> <topLayerName>”} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-bottomlayer “<netname> <bottomLayerName>”} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-viamodel <viaName>} — 指定 过孔 model.
\item \texttt{-x} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-y} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-no\_adjust} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：, adds via/stackvia at the center of the）
\item \texttt{-cut\_box\_only} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<instName>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-master <cellName>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-connect \{<pin1 net1 pin2 net2 ...>\}} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-x <x\_loc> -y <y\_loc>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-count <xcount> <ycount>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-pitch <x\_pitch> <y\_pitch>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-orient [ N|S|E|W|FN|FS|FE|FW ]} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-inst\_name} — 删除 实例 specified
\item \texttt{-all -cell} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<handle>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<metalLayer>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-x <x> -y <y>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<instName>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-index <from\_aF} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<metalLayer>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-x <x> -y <y>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\end{itemize}

\textbf{示例：}
\begin{lstlisting}
eco add pad -metal metal6 -type power -x 14 -y 2 -r 100 -l 1 -c 1
eco delete switch sw -index 1:2 2:4
eco delete via [<handle> | <metalLayer> -x <x> -y <y>]
[<handle> : specifies the “handle”
<metalLayer> : specifies the metal layer name for the via deletion
-x <x> -y <y> : specifies the x,y coordinates for the via to be deleted
\end{lstlisting}

\subsection{\cmd{export}}
\subsubsection*{export}

以第三方工具可读格式导出 RedHawk 数据。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
export [ db | eco | gridcheck | guiconf | res_calc ] ? <options> ?
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{<eco\_filename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-hier} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-def} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-cell \{a b c ... \}} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-def -cell \{a b c ... \}} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-def -def2pr} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ploc <ploc\_filename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-gds\_map <gds\_layer>.map} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<output\_filename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\end{itemize}

\subsection{\cmd{fao}}
\subsubsection*{fao}

焊盘阵列优化（FAO）：在网格上添加/优化 P/G 焊盘布局。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
fao add pad <options>
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{-window \{ \}} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-metal} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（必需）
\item \texttt{-net} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（必需）
\item \texttt{-pitchX -pitchY} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-r -l -c} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\end{itemize}

\subsection{\cmd{generate}}
\subsubsection*{generate}

生成供复查或后续流程使用的指定文件。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
generate [ msdf | simulationdeck | lef2spice | pinmap | mycyc_stats ] ? <options> ?
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{-i <sdfFileName>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o <msdfFileName>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-isdf} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-min, -max, -typical} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-dvd <dvd\_threshold>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-w “<logical\_module\_name>” “<physical\_module\_name>”} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-mmx} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-aplmmx\_cfg <filename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-sim\_time <time>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-inst <inst\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（必需）
\item \texttt{-xtor\_list <filename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-dir <output\_dir>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-aplmmx\_run\_dir <run\_dir\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-c <cell list filename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-s <subckts filename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-l <lefs filename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o <output filename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：lef2spice）
\item \texttt{-i <extracted\_pg\_network>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-s <subckt\_file>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o <output filename>} — 指定 output 文件. 默认: 引脚.map（默认：pin）
\end{itemize}

\textbf{示例：}
\begin{lstlisting}
generate msdf -w “” “top/block_1/” -i file.sdf -o file.msdf
generate msdf -w “top/block_2/” “” -i file.sdf -o file.msdf
\end{lstlisting}

\subsection{\cmd{get}}
\subsubsection*{get}

查询并获取设计对象属性（单元、网络、焊盘、开关等）。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
get [ analysis_mode | build | cell | celltype | current | design | inst | instbynet | instofcell |
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{-layer} — 指定 过孔 cut 层 name
\item \texttt{-bbox} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-space} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：is）
\item \texttt{-limit <N>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\end{itemize}

\subsection{\cmd{gsr}}
\subsubsection*{gsr}

读取、设置或转储 GSR 关键字值。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（必需）

\textbf{语法：}
\begin{lstlisting}
gsr [ get <GSR keyword> | [ set | append ] <GSR keyword> {
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{<GSR keyword>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<keywordValue>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<filename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\end{itemize}

\textbf{示例：}
\begin{lstlisting}
gsr set VCD_FILE {{top file.vcd} {FRONT_PATH “testbench/”}
import gsr <>.gsr
gsr set LIB_FILES { .lib CUSTOM }
gsr set DEF_FILES {{block1.def block} {block2.def block} {top.def top}}
gsr set VDD_NETS {{VDD <vdd_value> { <equiv_Net> } }
{VDDQ <vddq_value> { <equiv_Net> }}
gsr set VDD_NETS {{VDD 1.1 {VDD1CORE }} {VDDQ 3.3{VDDQ1 } }
gsr get VDD_NETS
\end{lstlisting}

\subsection{\cmd{history}}
\subsubsection*{history}

显示或管理当前会话的 TCL 命令历史。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\subsection{\cmd{import}}
\subsubsection*{import}

导入 GSR、LEF、DEF、SPEF、STA、工艺、VCD/FSDB 等输入文件。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
import [ apl | avm | db | def | eco | gsr | gsc | gridcheck | guiconf | keybinding | lef | lib | pad |
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{-c} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<dir\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-cache\_mode [ 0 |1]} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-cache\_dir <cacheTempDir>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ignore\_layout} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-vddonly | -vssonly} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<imputFileName>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<rule\_name1> ...} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-exact/-glob/-regexp} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：exact）
\item \texttt{-outDir} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-cacheEM} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：0 (no reuse)）
\item \texttt{-EMScale} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：1）
\item \texttt{-EMRuleSet} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-check\_only} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-use\_min} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-use\_typ} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：)）
\item \texttt{-use\_max} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\end{itemize}

\subsection{\cmd{license\_get}}
\subsubsection*{license get}

选择并持有特定类型的 RedHawk 许可证。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
license get <license_type>
\end{lstlisting}

\subsection{\cmd{marker}}
\subsubsection*{marker}

在版图中添加或删除十字准线标记。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
marker [ add | addfile | clearall | delete ] <options>
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{<x> <y>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-name <marker\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-color <color>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：yellow）
\item \texttt{-size <size>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-instance} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-name} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-instance} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-position} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-name} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\end{itemize}

\subsection{\cmd{mesh}}
\subsubsection*{mesh}

修改电源网格（绑带、过孔、电阻等）。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
mesh [ add | delete | fix | generate| optimize | snscalc | sub_grid| set_ width | vias ] ? <options>
\end{lstlisting}

\subsection{\cmd{message}}
\subsubsection*{message}

显示 Error、Info、Warning 消息信息。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
message [ disable | enable | info | list | config] ? <options> ?
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{<message\_id>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<list of ids>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<list of ids>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\end{itemize}

\textbf{示例：}
\begin{lstlisting}
message config -type [fatal | error | warn | info] <id list>
\end{lstlisting}

\subsection{\cmd{movie}}
\subsubsection*{movie}

设置或播放基于实例/晶体管引脚的动态波形动画。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：is an instance-
-zoom : makes the movie）

\textbf{语法：}
\begin{lstlisting}
movie [ make | play ] ? <options> ?
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{-transistor} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：is an instance-）
\item \texttt{-zoom} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-time <t1> <t2>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-step <time\_step>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-nolegend} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：on the）
\item \texttt{-high\_light\_switching\_instances} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-exit} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-dir <path>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\end{itemize}

\subsection{\cmd{perform}}
\subsubsection*{perform}

执行核心分析：功耗计算、提取、DvD、EM、热、ESD 等。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
perform [ analysis | clampcheck | cmmcheck | cpa | emcheck | esdcheck | extraction |
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{-lowpower} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-alp3d} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-static} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-dynamic} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-vcd -t <start\_time> <end\_time>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-vectorless (or “-dvd”\}} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-signalEM} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-jitter} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-scan} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-high\_capacity 1} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-mcycle} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-pwcap <pwcap\_file>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-simtime <sim\_time>} — 指定 仿真时间 in picoseconds
\item \texttt{-ipwr\_only} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-early} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-nx -ny} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-sir} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o} — 指定 output 文件name
\item \texttt{-clamp} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-bump} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-instConn} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-isolatedBump} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-cell <cell\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-celltype <type>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-inst <inst\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-volt <voltage>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-net <net\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-netConn} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-allNetConn} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-bumpConn} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-allBumpConn} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-loop} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-loopLength/ -b2bLoopLength} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-roff} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-rptDisConn} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-rule} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-esdStage} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-detail} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-append} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-summary} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-mode} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-net} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-skipcmm <cell.list>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-rule <rules\_file>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-clamp <clamp\_filename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-statusCheck} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：the report is dumped into the）
\item \texttt{-ruleType <rule\_type>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ruleName <rule\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-thread <num\_threads>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-optimize <mode>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-detail} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-append} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-excel} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-outDir <results\_dir>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：adsRpt）
\item \texttt{-ignoreError} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-saveDBDir} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-jobCount} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-collate} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-jobFile} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-slaveOnly} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-hostOnly} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-incremental} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-from / -to} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-zapI <I>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：1A)）
\item \texttt{-zapIRange <level1, level2...>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-zapV <V> -zapR <R>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：is to use）
\item \texttt{-zapIRange <level1, level2...>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-extendClampConn} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-addNode} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-reportByPGArc} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-power} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ground} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-clock} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-spice} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-l} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-c} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-nets} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o <output\_filename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：name: adsRpt/apache）
\item \texttt{-ignore\_floating} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-limit <max\_lines>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-perArc} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-stdcell / -macro} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-package} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-lumpSwitch'} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-all\_die} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-die} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-append} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-box <bbox>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-printPin} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-force} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ignoreInterPloc} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-byDistance} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-allPintype} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-inst} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-pin} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-pad} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-clear} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-fromClamp} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-logicPin} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-viaInst <} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-viaSwitch} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-lumpSwitch} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-from/-to} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：file adsRpt/res\_path）
\item \texttt{-addNode} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-width} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-total\_res} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-total\_vol} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-simplified\_report} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-append} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-esdFail <esd\_type> <N>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-multipath} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-multipath -gui} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-nodeSel} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-effR} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-consolidatedR} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-set effR [On | Off ]} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：value of effR for the session, calculati）
\item \texttt{-set nodeSel <options>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：option value of nodeSel for the session,）
\item \texttt{-set fromClamp} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-byDistance} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-allPintype} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-net} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-wirebond} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<flip chip partitions>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-cdie} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-static} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-lowpower} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-esd} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-parasitic} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-vcd} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-pincurrent} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-rleak} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-rleak\_par} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-solver mor} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：‘solver ac’ function, which is an accura）
\item \texttt{-plocname} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ind} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-no\_afs} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：AFS function）
\item \texttt{-passive} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-noglobal\_gnd | -global\_gnd} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-high\_capacity} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：)）
\item \texttt{-repeat\_current} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-probe} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-internal\_node} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-cell\_file <cell\_list\_filename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-reportcap} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o <output\_filename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：- PowerModel）
\item \texttt{-reuse} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-io} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-instance} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<name\_list>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-instFile} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-cell} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-cellFile} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-box <llx lly urx ury>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-net} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-layer} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-thread} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-mcore} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-allPoint} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-loopmode} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-incremental} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-append} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-verbose} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-limit <num\_lines>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：is）
\item \texttt{-o <file>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：- adsRpt/<design\_name>）
\item \texttt{-constrFile} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-optimize <mode>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-package} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-excludeCell <cell\_Name(s)>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-excludeInst <inst\_Name(s)>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-excludeCellFile <file\_Name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-excludeInstFile <file\_Name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-excludeDecap} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-xtor} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-pin} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-allPin} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-guardring} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-from \{<ptn>\}/ -to \{<ptm>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-addNode} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-shortAll} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-fromFile/-toFile} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-fullchip} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-detail} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-layer} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-self\_heat} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-inst} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-d} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ctmfilecheck} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-mt} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\end{itemize}

\textbf{示例：}
\begin{lstlisting}
# <Net> <Bump#> <Pass#> <Fail#> <OutRadius#> <Isolated#>
# <Net> <Pin#> <Connected#> <Unconnected#>
perform thermalmodel -layer ?-self_heat? ?-inst?
?-o <output_file>? ?-d <Thermal dir> ? ? -ctmfilecheck ? ? -mt ?
-d : a <Thermal dir>.tar.gz file is generated in the thermal directory. If no -d <Thermal dir> is
\end{lstlisting}

\subsection{\cmd{pfs}}
\subsubsection*{pfs}

PathFinder 静电放电（ESD）检查相关功能。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
pfs [ add | delete | export | import | show ] ? <options>?
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{-pin <pin\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-pinpair <pinpair\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-type <tname>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-layer <layer\_names>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-region <x1> <y1> <x2> <y2>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-locID} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-inst <instName>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-margin} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-includeAll} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-finX/-finY <num\_fingers>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-finX/-finY} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-finX/-finY 1} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-finX/-finY N (N > 1)} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-par <partition\_size>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-dist <um>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-multiPair} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-fwdR <forward\_res>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-bwdR <backward \_res>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-perFwdR /-perBwdR} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ivname<iv\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-all} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-type} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-name} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-all} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-cell} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-pin} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-pinpair} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-mergeDB} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-setupClamp} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-esdCell’} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-shortNode} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-fwdR <forward\_res>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-bwdR <backward \_res>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ivname<iv\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-shorting} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-pinPair} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\end{itemize}

\subsection{\cmd{plot}}
\subsubsection*{plot}

生成图形化波形或结果曲线文件。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
plot [ charge | current | rect | voltage | scatter | switching | line | analysis ] ?<options> ? ?-o
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{-o <outputFileName>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：- none）
\item \texttt{-net -power |-ground} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-switch} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-pad} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-virtual} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-name <name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-instance} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-range <lowerNum> <upperNum>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-overlay} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ldo} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-sv} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-xgr} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-nograph} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-png/-gif} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-region} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-fft} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-start\_time <time>, stop\_time <time>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-npts <number>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-fs <frequency>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：is Hz）
\item \texttt{-window} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-position} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-fill} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-color} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：white)）
\item \texttt{-name <inst\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-xtorname <xtor\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-file <filename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-pad | -vdd | -gnd} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ext | -int} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-virtual} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-sv} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-xgr} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-png/-gif} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-netname <list of net names>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-pinname <list of pin names>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ldo} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o <output\_file>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-fft} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-start\_time <time>, stop\_time <time>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-npts <number>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-fs <frequency>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：is Hz）
\item \texttt{-window} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-path} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-net} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-reset} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：appearance）
\item \texttt{-clearall} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-instance <list of instance names>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-position <start\_x start\_y end\_x end\_y>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-color <color>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-width <width>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-type [ StaticIR | StaticEM | DvD| esd|DynEM |signalem]} — 指定 type of 分析
\item \texttt{-emmode [ ave | rms | peak ]} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-layer [<layer\_name> | ALL ]} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-net [<netname> | ALL ]} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-instance [avgTW | minTW | maxTW | minCyc | instDrop | vddDrop | vssBounce]} — for 实例
\item \texttt{-lowerLim <lower>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-upperLim <upper>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o <filename>} — 指定 output 文件name.
\item \texttt{-ruleType} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ruleName} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-nograph} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\end{itemize}

\subsection{\cmd{print}}
\subsubsection*{print}

打印基于文本的分析报告。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
print [ charge | decap | dvd | instance | inst_data | memory | mmx_decap_report | statistics |
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{-o <outputFileName>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：- output displayed in the terminal windo）
\item \texttt{-used} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o <outputFileName>} — 指定 output 文件name (no 默认 文件)（默认：file)）
\item \texttt{-limit [<number> | all]} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<instance\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\end{itemize}

\subsection{\cmd{probe}}
\subsubsection*{probe}

在仿真前选择/取消选择探测节点。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
probe [ add | delete | get ] ? <options>?
\end{lstlisting}

\textbf{示例：}
\begin{lstlisting}
probe add 1023.2 3457.1 Met1 nodeABC
plot current -probe -name <node_name>
plot voltage -probe -name <node_name>
probe get nodetest -current
probe get <probe-name> -location
probe get test1 -location
probe get <probe-name> -voltage
\end{lstlisting}

\subsection{\cmd{query}}
\subsubsection*{query}

查询设计对象并返回属性信息。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
query [ top | inst | simulation_time | location ] ? <options> ?
\end{lstlisting}

\subsection{\cmd{report}}
\subsubsection*{report}

生成功耗、EM、IR 压降等文本报告。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：)）

\textbf{语法：}
\begin{lstlisting}
report [ block_power_assignment | clocknetwork | cmmpin | dvd | esdcheck | ir | leafinst | net |
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{-o <output\_file>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-root <trace\_start\_pin>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-leaf <trace\_end\_pin>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-block <block\_inst\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-dumpLeaf} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-detailArc} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-routing} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-instance} — 生成 an 实例-based 电压跌落 report
\item \texttt{-net} — 指定 网络 to be reported. All 网络 are reported 默认情况下
\item \texttt{-bbox} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-layer} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-threshold} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-limit} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-precision} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：3 digits, but）
\item \texttt{-dvd\_sort} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-type} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：is all types）
\item \texttt{-name} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-b2cName <b2c\_rule\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-glob|-regexp} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-outDir} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：is adsRpt/ESD）
\item \texttt{-reportB2C/-reportC2B} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-arcR} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（必需）
\item \texttt{-loopR} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（必需）
\item \texttt{-parallelR} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（必需）
\item \texttt{-append} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-excel} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-cell} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-failedOnly} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-detail} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-netPairVth} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-peakVolt} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-diffVolt} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-reportByPGArc} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-summary} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-sort} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-excel} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：, summary data is）
\item \texttt{-pg\_volt} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o <filename>} — 指定 output 文件name
\item \texttt{-f <input\_file>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-inst} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-region <corner locations>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-region\_name} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-xtorlist} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-net} — 指定 网络 names. 默认 is to report all 网络.（默认：is to report all nets）
\item \texttt{-mmx\_only} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-all} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o <file>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：filename is adsRpt/）
\item \texttt{-loop} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-constraint <constraint\_filename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-o <output\_file>} — 指定 output 文件name
\end{itemize}

\textbf{示例：}
\begin{lstlisting}
report esdcheck -detail -perNet -type B2C -name abc* -glob
report esdcheck -type B2C -name {M_test_[3-5]} -regexp
# <ARC_R> <x,y loc> <layer> <domain> <pin> <inst> <clamp> <x,y loc> <layer>
\end{lstlisting}

\subsection{\cmd{ring}}
\subsubsection*{ring}

创建或修改模块/芯片周围的电源/地环。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\subsection{\cmd{route\_fix}}
\subsubsection*{route fix}

在指定层上添加修复布线以改善 IR。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
route fix [ -from { x1 y1 x2 y2 <layer>} | -from_pt { xf yf <layer>} ]
\end{lstlisting}

\subsection{\cmd{select}}
\subsubsection*{select}

在 GUI 中选择并高亮设计对象。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
select [add | addfile | clear | clearall | get ] ? <options> ?
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{<object>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-linewidth <width>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-color <color>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-name <groupname>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<FileName>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\end{itemize}

\subsection{\cmd{setup}}
\subsubsection*{setup}

设置设计数据与运行条件（design、package、pad 等）。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（必需）

\textbf{语法：}
\begin{lstlisting}
setup [analysis_mode | .apache | apl | cpa | design | fsdb | package | pad | pss | switchres |
\end{lstlisting}

\textbf{选项说明：}
\begin{itemize}
\item \texttt{-dmp} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-dir <output\_directory>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-thermal} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-power} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ctm} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-mhs} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-neverflip} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-net} — 指定 网络 or 网络 list
\item \texttt{-float} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-volt} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<gsrFilename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-c <C\_pF>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：0）
\item \texttt{-ignore} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-r <R\_Ohms>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：0）
\item \texttt{-l <L\_pH>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：0）
\item \texttt{-ignore} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-power} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ground} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-c <c\_value>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：0）
\item \texttt{-r <r\_value>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：0）
\item \texttt{-pad\_file <filename>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-subckt <pss\_ckt\_name>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{<file>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-clear} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-Ron} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-cmmOnly} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ignore} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-power} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-ground} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。
\item \texttt{-c <c\_value>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：0）
\item \texttt{-l <l\_value>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：0）
\item \texttt{-r <r\_value>} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：0）
\end{itemize}

\textbf{示例：}
\begin{lstlisting}
setup pss -pad_file test_pss.ploc -subckt pkgA.sp
\end{lstlisting}

\subsection{\cmd{show}}
\subsubsection*{show}

在 GUI 中显示指定结果的色阶图。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
show <map_type>
\end{lstlisting}

\subsection{\cmd{window}}
\subsubsection*{window}

控制 GUI 窗口布局与视图配置。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
window ? geometry ?<x> <y>? ?<width> <height>? ? cmd?
\end{lstlisting}

\subsection{\cmd{zoom}}
\subsubsection*{zoom}

缩放版图视图至区域、对象或全芯片。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{语法：}
\begin{lstlisting}
zoom [ in | out | top | rect <x1 y1 x2 y2> ]
\end{lstlisting}

\subsection{\cmd{perform} 子命令概要}
\subsubsection*{perform Subcommand Summary}

\begin{longtable}{@{}p{0.26\textwidth}p{0.68\textwidth}@{}}
\toprule
\textbf{子命令} & \textbf{说明} \\
\midrule
\endhead
\cmd{perform analysis} & 执行指定类型分析（静态/动态 IR、EM 等） \\
\cmd{perform extraction} & 执行 R/C 电源网格提取 \\
\cmd{perform pwrcalc} & 功耗计算 \\
\cmd{perform powermodel} & 生成功耗模型 \\
\cmd{perform res_calc} & 电阻/网格电阻计算 \\
\cmd{perform gridcheck} & 电源网格 DRC/连通性检查 \\
\cmd{perform clampcheck} & ESD clamp 检查 \\
\cmd{perform thermalmodel} & 热分析建模 \\
\cmd{perform jitter} & 抖动分析 \\
\cmd{perform min_res_path} & 最小电阻路径分析 \\
\cmd{perform esdcheck} & ESD 检查 \\
\cmd{perform dvd} & 动态电压跌落（DvD）分析 \\
\bottomrule
\end{longtable}

\section{RedHawk 图形用户界面}
\subsection*{RedHawk Graphic User Interface Description}

\figplaceholder{Figure D-3}{RedHawk 图形用户界面布局}

GUI 主要区域包括菜单栏、主显示区、控制按钮、视图配置、结果查看、查询、TCL 命令行与日志区。

\subsection{鼠标操作}
\subsubsection*{Mouse Function}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
          0.000705042 A < 0.001 ^
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：values thereafter）

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
         Windows->Preferences->Advanced
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
    - “View Layers” (left button in the row) - displays the ‘Layer Select’ dialog box, as shown in Figure
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：to avoid interfering with the display of）

\begin{lstlisting}
    - “Set Color Range” (center button in row) - displays a Color Map dialog box based on the ‘View
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
    - Colors can be assigned to specific ranges of parameter values, such as static or dynamic
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：button allows easy return to the origina）

\begin{lstlisting}
    - “Show Power Pad” (right button in row) - toggles the display of power pads on and off (on by
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：)）

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
    - SA - “Show all” of layout - displays some or all objects in the design layout (individual layer
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：pins are turned off, layer and instance）

\begin{lstlisting}
  - IR - “IR Map” - displays the latest analysis results, static IR voltage drop or dynamic voltage
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：, while layers and vias are always shown）

\begin{lstlisting}
  - EM - “ElectroMigration Map” - displays results of electromigration analysis for static or dynamic
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：, and are shown in selected object color）

\begin{lstlisting}
  - CUR - “Current Map” - displays results of maximum current by node for static or dynamic
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
  - TP - “Thermal Profile” - displays a color-coded map of regional temperature for the layer
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
  - PD - “Power density Map” - displays per instance power density by grid. Clicking on the ‘Set
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
  - IPM - “Instance Power Map” - displays results of power analysis for all instances. Instance
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
  - CIP - “Clock Instance Power Map” - displays results of power analysis for clock instances.
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
  - LPM - “Leakage Power Map” - displays leakage power for all instances. Clicking on the ‘Set
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
  - IFM - “Instance Frequency Map” - displays the frequency of each instance from STA file
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：, each capacitance colormap is scaled ac）

\begin{lstlisting}
   - DD - ”Decap Density Map” - displays usable decap density, which includes intrinsic decap and
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
   - DEV - “Device Decap Density Map” - displays original intrinsic device decap density.
   - LC - “Load Cap Density Map” - displays load capacitance density.
   - IDD - “Inserted Decap Density Map” - displays inserted intentional decap density for cells
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
   - PG - “P/G Cap Density Map” - displays power/ground capacitance density.
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
   - ? - displays a ‘Query Objects’ dialog box that allows searching for design objects, specified
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
- P - displays an ‘Instance Property’ dialog, listing the properties of the selected instance (in
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：displays the sv Vdd and Vss waveforms fo）

\begin{lstlisting}
- CLR - clears all highlights, selections and rulers in the display (default function).
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
    - X,Y - describes the x,y coordinates of the cursor location
    - ll,ur - describes the x,y coordinates of the lower left and upper right corners of the view
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
          ctrl<key>d Button/Ob
          ctrl<key>p Button/Property
          ctrl<key>c Button/QueryCombo
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
         import keybind keybind.txt
\end{lstlisting}

\subsection{GUI 控制按钮}
\subsubsection*{GUI Control Buttons}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
         Windows->Preferences->Advanced
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
    - “View Layers” (left button in the row) - displays the ‘Layer Select’ dialog box, as shown in Figure
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：to avoid interfering with the display of）

\begin{lstlisting}
    - “Set Color Range” (center button in row) - displays a Color Map dialog box based on the ‘View
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
    - Colors can be assigned to specific ranges of parameter values, such as static or dynamic
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：button allows easy return to the origina）

\begin{lstlisting}
    - “Show Power Pad” (right button in row) - toggles the display of power pads on and off (on by
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：)）

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
    - SA - “Show all” of layout - displays some or all objects in the design layout (individual layer
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：pins are turned off, layer and instance）

\begin{lstlisting}
  - IR - “IR Map” - displays the latest analysis results, static IR voltage drop or dynamic voltage
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：, while layers and vias are always shown）

\begin{lstlisting}
  - EM - “ElectroMigration Map” - displays results of electromigration analysis for static or dynamic
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：, and are shown in selected object color）

\begin{lstlisting}
  - CUR - “Current Map” - displays results of maximum current by node for static or dynamic
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
  - TP - “Thermal Profile” - displays a color-coded map of regional temperature for the layer
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
  - PD - “Power density Map” - displays per instance power density by grid. Clicking on the ‘Set
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
  - IPM - “Instance Power Map” - displays results of power analysis for all instances. Instance
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
  - CIP - “Clock Instance Power Map” - displays results of power analysis for clock instances.
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
  - LPM - “Leakage Power Map” - displays leakage power for all instances. Clicking on the ‘Set
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
  - IFM - “Instance Frequency Map” - displays the frequency of each instance from STA file
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：, each capacitance colormap is scaled ac）

\begin{lstlisting}
   - DD - ”Decap Density Map” - displays usable decap density, which includes intrinsic decap and
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
   - DEV - “Device Decap Density Map” - displays original intrinsic device decap density.
   - LC - “Load Cap Density Map” - displays load capacitance density.
   - IDD - “Inserted Decap Density Map” - displays inserted intentional decap density for cells
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
   - PG - “P/G Cap Density Map” - displays power/ground capacitance density.
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
   - ? - displays a ‘Query Objects’ dialog box that allows searching for design objects, specified
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
- P - displays an ‘Instance Property’ dialog, listing the properties of the selected instance (in
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：displays the sv Vdd and Vss waveforms fo）

\begin{lstlisting}
- CLR - clears all highlights, selections and rulers in the display (default function).
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
    - X,Y - describes the x,y coordinates of the cursor location
    - ll,ur - describes the x,y coordinates of the lower left and upper right corners of the view
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
          ctrl<key>d Button/Ob
          ctrl<key>p Button/Property
          ctrl<key>c Button/QueryCombo
\end{lstlisting}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{lstlisting}
         import keybind keybind.txt
\end{lstlisting}

\subsection{GUI 菜单}
\subsubsection*{GUI Menu}

\subsection{文件菜单}
\subsubsection*{File Menu}

\textbf{File $\rightarrow$ Import Design Data} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{File $\rightarrow$ Import Database} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{File $\rightarrow$ Export Database} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{File $\rightarrow$ Import ESD DB} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{File $\rightarrow$ Export ESD DB} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{File $\rightarrow$ Import ECO} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{File $\rightarrow$ Export ECO} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{File $\rightarrow$ Import GUI Config} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{File $\rightarrow$ Export GUI Config} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{File $\rightarrow$ Playback} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{File $\rightarrow$ Exit} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\subsection{编辑菜单}
\subsubsection*{Edit Menu}

\textbf{Edit $\rightarrow$ Undo} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Edit $\rightarrow$ Redo} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Edit $\rightarrow$ Add Pad} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Edit $\rightarrow$ Delete Pad} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Edit $\rightarrow$ Add Power Strap} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：0）

\textbf{Edit $\rightarrow$ Edit Power Strap} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Edit $\rightarrow$ Add Via} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Edit $\rightarrow$ Delete Via} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Edit $\rightarrow$ Add Decap Cell} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Edit $\rightarrow$ Delete Decap Cell} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Edit $\rightarrow$ Edit Probe} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Edit $\rightarrow$ ESD Clamp ECO} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Edit $\rightarrow$ Chip Partition} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Edit $\rightarrow$ Ruler ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Edit $\rightarrow$ Single Key} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\subsection{视图菜单}
\subsubsection*{View Menu}

\textbf{View $\rightarrow$ Chip Layout Map} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ Nets} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：the signal and clock nets are not shown）

\textbf{View $\rightarrow$ Connectivity ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{List of PG Weaknesses $\rightarrow$ Instances} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{List of PG Weaknesses $\rightarrow$ Transistor Pins} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ Technology Layers} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ Hierarchy Level} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ EM Mode} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：, and for Power analysis, Peak is defaul）

\textbf{View $\rightarrow$ Map Configuration ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Windows $\rightarrow$ Preference -> Color Maps -> Allow More Color Spectrum feature is selected. This} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ Power Maps ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{commands by using the View $\rightarrow$ Map Configurations command for the display.} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ Resistance Maps} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{multiple Vdd/Vss designs use the View $\rightarrow$ Nets command to select the nets to be included in the} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ Voltage Drop Maps ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ Current Maps ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ Electromigration Maps} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：is 10）

\textbf{View $\rightarrow$ Transistor Pin Maps ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ Dynamic Instance DvD ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ Nets. Click on the ‘Set Color Range’ button in the Configuration group to obtain a ‘Voltage} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ Nets. Click on the ‘Set Color Range’ button in the Configuration group to obtain a ‘Voltage} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ Nets. Click on the ‘Set Color Range’ button in the Configuration group to obtain a ‘Voltage} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Displays the minimum dynamic Vdd - Vss difference over the whole cycle, for nets selected in View $\rightarrow$ } — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ Decap Maps ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{the display of the following map commands by using the View $\rightarrow$ Map Configurations -> Cap} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：is 10 steps)）

\textbf{View $\rightarrow$ ESD Resistance Lists ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{resistances. Choose View $\rightarrow$ ESD Resistance List -> List of Bump-to-Bump Para, as shown in} — 打开对应对话框或执行相应 GUI 操作。

\textbf{View $\rightarrow$ ESD Clamp Lists} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ ESD Resistance Maps ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ ESD Current Density->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ Impact on Timing Maps ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ Clock Jitter Maps (PJX)} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ Clock Jitter Maps ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{View $\rightarrow$ STA Critical Path} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\subsection{工具菜单}
\subsubsection*{Tools Menu}

\textbf{Tools $\rightarrow$ Lowpower ->} — 打开对应对话框或执行相应 GUI 操作。

\textbf{Power $\rightarrow$ Calculate} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Power $\rightarrow$ Import} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Tools $\rightarrow$ Signal EM ->} — 打开对应对话框或执行相应 GUI 操作。

\textbf{Power $\rightarrow$ Calculate} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Power $\rightarrow$ Import} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Tools $\rightarrow$ Chip Power Model ->} — 打开对应对话框或执行相应 GUI 操作。

\textbf{Power $\rightarrow$ Calculate} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Power $\rightarrow$ Import} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Tools $\rightarrow$ PJX Timing Paths ( was Fullchip Critical Paths PPX )} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Tools $\rightarrow$ Clock Jitter ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Fullchip Jitter $\rightarrow$ Analyze} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Fullchip Jitter $\rightarrow$ Long Term Jitter Report} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Fullchip Jitter $\rightarrow$ Long Term Cycle-2-Cycle Jitter Report} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Fullchip Jitter $\rightarrow$ Long Term Jitter Bottleneck Report} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Sign-off Jitter $\rightarrow$ Simulate} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Sign-off Jitter $\rightarrow$ Clock Jitter Report} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Sign-off Jitter $\rightarrow$ Clock Jitter Bottleneck Report} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Tools $\rightarrow$ PathFinder SOC} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（必需）

\textbf{Clamp Pin Setup $\rightarrow$ Add} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Clamp Pin Setup $\rightarrow$ Delete} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Clamp Pin Setup $\rightarrow$ Highlight} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Clamp Pin Setup $\rightarrow$ Import} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Clamp Pin Setup $\rightarrow$ Export} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Clamp Connectivity Check $\rightarrow$ Unconnected Clamp Devices} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Clamp Connectivity Check $\rightarrow$ Clamp Connectivity Filtering} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Clamp Connectivity Check $\rightarrow$ Net Connectivity} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Clamp Connectivity Check $\rightarrow$ Isolated Bump} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Tools $\rightarrow$ Fixing and Optimization (FAO) ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Global GSR Setting button $\rightarrow$ General Global GSR Setting tab} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Global GSR Setting button $\rightarrow$ Advanced Global GSR Setting tab} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Via Operation button $\rightarrow$ Add Vias tab} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Via Operation button $\rightarrow$ Delete Vias tab} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Mesh Operation button $\rightarrow$ Add Mesh tab} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Mesh Operation $\rightarrow$ Delete Mesh tab} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Decap Operation button $\rightarrow$ Decap Fill tab} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Tools $\rightarrow$ Effective Resistance Computation} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Static $\rightarrow$ Power ->} — 打开对应对话框或执行相应 GUI 操作。

\textbf{BPA $\rightarrow$ Import BPA} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{BPA $\rightarrow$ Export BPA} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{BPA $\rightarrow$ BPA in GSR} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{BPA $\rightarrow$ Instance List} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{BPA $\rightarrow$ Highlight Exclude Area} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{BPA $\rightarrow$ Power Map of Instances} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Static $\rightarrow$ Network Extraction} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Static $\rightarrow$ Pad Wirebond Package Constraints} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Static $\rightarrow$ Static Voltage Drop Analysis} — Executes static IR 电压跌落 分析.

\textbf{Static $\rightarrow$ EM Check} — 打开对应对话框或执行相应 GUI 操作。

\subsection{动态分析菜单}
\subsubsection*{Dynamic Menu}

\textbf{Dynamic $\rightarrow$ Power ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Dynamic $\rightarrow$ Network Extraction} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Dynamic $\rightarrow$ Pad Wirebond Package Constraints} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Dynamic $\rightarrow$ Dynamic Voltage Drop Analysis} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Dynamic $\rightarrow$ Vectorless Only Analysis} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Dynamic $\rightarrow$ VCD Only Analysis} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Dynamic $\rightarrow$ EM Check} — 打开对应对话框或执行相应 GUI 操作。

\textbf{Timing $\rightarrow$ Sign-off Clock Tree ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Timing $\rightarrow$ Sign-off Critical Path ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Timing $\rightarrow$ Generate MSDF} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\subsection{结果菜单}
\subsubsection*{Results Menu}

\textbf{Results $\rightarrow$ Log Message Viewer} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：), ‘CPU/Memory Usage’, ‘Setup Design’, ‘）

\textbf{Results $\rightarrow$ List of Effective Grid Resistances} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{the View $\rightarrow$ Nets menu command dialog.} — 打开对应对话框或执行相应 GUI 操作。

\textbf{Results $\rightarrow$ List of Effective Instance Resistances} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Results $\rightarrow$ List of Worst EM} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Results $\rightarrow$ List of Worst IR for Wires and Vias} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：a 5\% IR drop threshold is used to select）

\textbf{Results $\rightarrow$ List of Highest Power Instances} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Results $\rightarrow$ List of Metal-Via EM Ratio} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Results $\rightarrow$ List of Worst IR Instances (Static)} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Results $\rightarrow$ List of Worst Instance DVD} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Results $\rightarrow$ List of Worst Transistor Pin Voltages} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Results $\rightarrow$ Analysis Histograms} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Results $\rightarrow$ Movie ->} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{To create a DvD movie for selected P/G nets, first use the menu command View $\rightarrow$ Nets, to select or} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Explorer $\rightarrow$ Generate} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Explorer $\rightarrow$ View Results} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\subsection{窗口菜单}
\subsubsection*{Windows Menu}

\textbf{Windows $\rightarrow$ Multiple Pages} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：View is always available from the page v）

\textbf{Windows $\rightarrow$ Preferences} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。（默认：font (Bitstream Charter-9-Normal) defaul）

\textbf{- Sync all instance based IR map settings, such as IIR and the maps under View $\rightarrow$ } — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Windows $\rightarrow$ Detach View Bar} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Windows $\rightarrow$ Detach Message Window} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Help $\rightarrow$ About} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Help $\rightarrow$ Manual} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{\# Ctrl-d invokes File $\rightarrow$ Import DB dialog box} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{4.   Enable the specified single keybinding shortcuts by clicking on the Edit $\rightarrow$ Single Keybinding} — GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\textbf{Edit $\rightarrow$ Single Keybinding menu command, or use the Ctrl-T shortcut.} — 打开对应对话框或执行相应 GUI 操作。

\subsection{回放与其他 GUI 功能}
\subsubsection*{Playback and Other GUI Features}

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

GSR 关键字 ：用于设置与  相关的 RedHawk 运行参数。

\begin{seeAlsoBox}
完整 GUI 控件说明见原书附录 D 及第~3~章 GUI 概述。
\end{seeAlsoBox}
